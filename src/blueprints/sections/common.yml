type: fields
fields:
  title_short:
    label: Short title
    type: text
  subtitle:
    label: Subtitle
    type: text
  section:
    label: Section
    type: select
    options: query
    query:
      fetch: site.page(sections).children
      text: "{{ page.title }}"
      value: "{{ page.uid }}"
  links:
    label: Links
    type: textarea
